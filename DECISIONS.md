# DECISIONS

Project-level product and architecture decisions. Status: `OPEN` | `LEANING` | `DECIDED`.

## Product

| ID | Decision | Status |
|----|----------|--------|
| P0 | Product name is **spar** (binary `spar`) | DECIDED |
| P1 | spar is a **first-class agent product** (TUI-first), not a Pi/Claude plugin | DECIDED |
| P2 | Default human surface is a **nice fleet TUI** (Claude/Grok/agy class); CLI/JSON for outer agents | DECIDED |
| P3 | Discovery via **built-in skills + AGENTS.md** (agent-browser pattern) | DECIDED |
| P4 | Dual execution: **native-cli** + **api-sdk**, one orchestrator | DECIDED |
| P5 | First-class **swarm bus** (A2A), not dumb mailbox only | DECIDED |
| P6 | **Dynamic model select** (vals benchmarks + prefs/urgency) is a first-class product path alongside explicit `--providers` | DECIDED — see MS* |
| P7 | Operator model: the chat session is **disposable**, its job ends at launch; spar owns a run's lifetime and `.spar/runs/<id>/` is the system of record, so any session gets current by reading it. See `docs/architecture-operator-model.md` | DECIDED |

## Orchestration

| ID | Decision | Status |
|----|----------|--------|
| O1 | **One run id** plan → implement → ship | DECIDED |
| O2 | Always **worktrees** for coding slots | DECIDED |
| O3 | Cleanup **auto when fully done**, fail-closed if still needed | DECIDED |
| O4 | bwrap **per-provider optional**; none enabled by default yet | DECIDED |
| O5 | Default implement: **1 impl + ≥2 reviewers**; multi-impl in **arena** | DECIDED |
| O6 | Arena finish: **winner** or **reconcile** then review | DECIDED |
| O7 | Roles + peer are **v1 first-class** | DECIDED |
| O8 | Plan: multi-provider + critic; **structured/big** for large features | DECIDED |
| O9 | Gates **configurable**; lean high autonomy | DECIDED |
| O10 | Ship: **draft PR**, never merge; force-with-lease only swarm branches | DECIDED |
| O11 | Stream **everything** (logs/events) | DECIDED |
| O12 | Headless default; tmux **namespaced opt-in only** | DECIDED |
| O13 | Quota: **best-effort auto** + manual; Claude rate_limits-shaped signals | DECIDED |
| O14 | **Suite channel** (implement/loop): dedicated cheap `tester` slot runs full suites; impl/review smoke/diff-only when suite ran; long `suite.timeout_secs`; salvage partial review/suite artifacts on timeout; fail closed if enabled but no tester/provider. Independent `review` workflow may still run its own tests (no suite slot by default) | DECIDED |
| O15 | **Spec / test-author** (plan flow): after planner+critic write `plan.md`, a separate `TestAuthor` slot freezes acceptance tests **before** coding and before the plan gate. Not planner, not critic, not suite `tester`. **Artifact-first** (plan + critique); bus is audit trail (no live multi-turn with finished planner/critic). Outputs `artifacts/test-contract.md` + tests in worktree; implement **always overlays** author working tree into impl (plus best-effort branch merge; abort on conflict). Fail closed if author fails, contract missing, or apply fails when TestAuthor ran. Config `[spec]` (`enabled` default true, optional `provider`, `timeout_secs`). Human gate is plan + contract | DECIDED |
| O16 | **Clean exit ≠ success.** A slot's `expected_artifact` must exist and be non-empty or the slot is `Failed` (salvage its log tail), on the parallel path as well as the sequential one. A reviewer that exits 0 without producing a review no longer counts as a review | DECIDED |
| O17 | **Stall = log-quiet + (process gone OR past role budget).** The `bus` heartbeat is process-liveness, not progress (a live child beats every ~30s regardless of work), so gating `stalled` on the heartbeat alone would disable the wedged-alive alarm for the headless default. Dual threshold: a running slot is `stalled` when log-quiet past `stall_warn_secs` AND either its heartbeat is also quiet (dead/gone) OR it has been log-quiet for its whole role timeout (alive but hung). Busy-but-log-quiet agents (streaming-json mid tool-call) inside budget are not stalled. `stalled` is advisory only; a true hang still hits the role timeout → `Phase::Stuck` → exit 3 | DECIDED |
| O18 | **`cli:codex` adapter** (codex `exec --json`). `DeliveryStrategy::None` + `PresenceSource::None`, no takeover, no resume. FullAuto uses `--dangerously-bypass-approvals-and-sandbox` (unsandboxed like the other adapters; the worktree is the boundary) + `--skip-git-repo-check`. codex JSONL works: the stream coalescer parses `item.completed` (agent_message/reasoning/tool items) and `turn.completed.usage` (`input_tokens`/`cached_input_tokens`/`output_tokens`) for **real token tracking**. **Backend + model** come from a codex *profile* (its own (backend, model) bundle) since spar's provider ref can't carry a model and there's no inline `--model` flag; selection mirrors `api:` env style — `--select` model → `SPAR_CODEX_MODEL` (`-m`) → default; `SPAR_CODEX_PROFILE` picks `-p` (unset → `muse` = OpenRouter+Muse Spark; empty → omit `-p` for codex's own default; else that profile). OpenRouter profiles need `OPENROUTER_API_KEY` exported (it is `set -Ux`; codex reads it via `env_key`). Rejected per-slot model-in-ref (needs provider-ref/model rework — MS14-D/X6, deferred) and a `[codex]` config block (env suffices now; add later if per-project persistence is wanted). Opt-in only — **not** in `default_provider_order`. (kilo adapter was prototyped and dropped: kilo 7.4.11 `--format json` is broken — no stdout, hangs — so no token tracking was possible) | DECIDED |
| O19 | **Unmet acceptance criterion ≠ success.** Reviewers emit `## Acceptance` with one `AC-n: pass\|fail\|unverified — evidence` line per criterion in `test-contract.md`. Any `fail`, or any contract `AC-n` absent from the review, forces `request_changes`. `unverified` blocks by default; relax with `[review].require_all_criteria = false`. Extends O16 | DECIDED |
| O20 | **`## Verdict` is an anchored header, not a substring.** The first non-blank line under the first `## Verdict` section must be `approve` or `request_changes`; missing or unparseable ⇒ `request_changes` (fail closed). Replaces the whole-document scan that made the format block's own `approve \| request_changes` line self-blocking. One parser (`workflow/review_result.rs`) serves both implement and arena | DECIDED |
| O21 | **Reviewers see the plan and the contract.** `plan.md` and `test-contract.md` bodies are passed to the reviewer template, not summarized as a prose nudge. A reviewer that cannot see the criteria cannot verify them | DECIDED |
| O22 | **`[roles]` assigns providers by role, not position.** A top-level `[roles]` block maps `planner`/`plan_critic`/`implementer`/`reviewer` (a list)/`tester`/`test_author` to `@model`-capable provider refs, validated through `ProviderRef::parse` at config load (errors name the offending role key, so the `.expect()` in `init_slot_model` stays unreachable from user input). The old positional `[suite].provider` / `[spec].provider` are **removed** (clean break, no shim): `tester` and `test_author` replace them. Priority 8 lands the data model + plumbing; Priority 9 rewires the fleet to consume it. Distinct from `[model_select.role_profiles]`, which maps a role to a benchmark *profile*, not a provider | DECIDED |
| O23 | **One role vocabulary = `SlotRole` snake_case.** `SlotRole::as_config_key()`/`from_config_key()` are the single source of truth (an inline test asserts they agree with the serde `rename_all = "snake_case"` representation for every variant). `critic` is retired for `plan_critic` and the plan flow's overloaded `tester` label becomes `test_author`, freeing `tester` to mean only the cheap suite runner. No aliases — accepting `critic` is how two vocabularies become three. `[model_select.roles]` is renamed to `[model_select.role_profiles]` so the top-level `[roles]` (providers) and the model-select role→profile map no longer collide | DECIDED |
| O25 | **`cli:opencode` adapter, OpenRouter-default.** New native-cli adapter (`opencode run --format json --dangerously-skip-permissions [-m …] -- <prompt>`). Model routing prepends `openrouter/` to a bare vendor slug (`cli:opencode@meta/muse-spark-1.1` → `-m openrouter/meta/muse-spark-1.1`), passes an explicit `openrouter/…` or a bare word through unchanged — so OpenRouter is the **default** coder route. Token tracking parses opencode's per-step NDJSON, deduping the dash/underscore double-emit by `(normalized type, part.id)` so `step_finish` output is not counted twice (input=max, output=sum, cache=max). v1 is agy-tier (`DeliveryStrategy::None` / `PresenceSource::None`); opencode's SSE event stream + `session.prompt()` could make it first-class (`SdkPrompt` / `Sse`) later — reserved, not wired. **opencode is the recommended OpenRouter coder over `cli:codex`** (~half the per-turn token overhead: ~14.6k vs ~29.5k input on the same trivial task + model); codex stays the alternative | DECIDED |
| O24 | **Role resolution precedence + one resolver.** Fleet assignment runs through a single `workflow::roles_resolve::provider_for(role, idx, fleet, cfg)` with fixed precedence **explicit `--providers` (positional one-off) > `[roles]` > `[providers].order`**. A populated `[roles]` synthesizes the fleet in `model_select::resolve_providers`, so it *satisfies* the "`--providers` or `--select` is required" invariant (consolidated to that one choke point; the redundant `implement.rs` guard and dead `CommonOpts::require_providers` are deleted). `[roles].reviewer` is a list indexed per reviewer; reviewer **widening** and implementer **rotation** draw candidates from `[roles]` then `[providers].order` then the live fleet instead of a hardcoded provider list. `tester`/`test_author` keep their dedicated resolvers (`[roles]` head-of-chain with a usability check, falling through when unusable) | DECIDED |
| O26 | **A run has one base commit, taken from the invoking directory.** Slot worktrees are cut from `state.base_commit`, resolved once at run creation as `--base <ref>` (branch/tag/sha/`origin/main`; unresolvable = hard error, never a silent fallback) else the HEAD of the cwd spar was invoked from. Not `project_root`'s HEAD: `find_project_root()` deliberately maps a linked worktree to the main checkout so `.spar/` stays one per repo, which meant the documented one-agent-per-worktree setup handed every slot the main branch while the run looked healthy. Recorded as `base_ref`/`base_commit` in `state.json`, run JSON and `status --json`, and printed at launch — a wrong base has to be *visible*, since agents on the wrong branch report cleanly and find nothing. `implement --run <id>` inherits it; a run **cannot be re-based** after creation (the plan phase's frozen tests are overlaid wholesale into the implementer, so straddling two bases yields a branch parented on the new base carrying the old base's contents) — plan a new run instead. `ship` targets its draft PR at the run's base branch when that branch exists on `origin`, else the repo default, overridable with `ship --base` | DECIDED |
| O27 | **A run is bound to the config it was created with.** `spar.toml` is one mutable file at the project root (the main checkout) read fresh by every process, while only `isolation`/`autonomy`/`message_budget`/`providers` were snapshotted into `state.json` — so `[roles]`, `[timeouts]`, `[spec]`, `[suite]`, `max_agents` and `[review].require_all_criteria` were live reads, and `implement --run` re-resolved the fleet from whatever the file said at resume. With one agent per worktree all sharing that file (and the operator policy telling agents to write `[roles]` into it before launching), concurrent runs silently re-configured each other, up to and including loosening another run's ship gate. Each run now freezes the merged config at creation into `.spar/runs/<id>/config.json`; every run-scoped command reads the snapshot, never the live file. `implement --reload-config` is the only escape hatch and re-freezes rather than bypassing. `--role <role>=<provider>` (repeatable) assigns per role on the command line so agents never need to write the shared file at all — it is the reason they were writing it, since `--providers` is positional only. Precedence: `--providers` > `--role` > `[roles]` > `[providers].order`. Pre-O27 runs fall back to the live file. Deliberately out of scope: the TUI's stall *display* thresholds still come from the project config, since it renders many runs across projects and per-run config I/O per frame buys nothing — run execution is unaffected | DECIDED |
| O28 | **Abandoned runs are surfaced and swept, never auto-reaped.** Slots spawn into their own process groups (so slot timeouts reap nested suites), so they outlive their orchestrator: a driver's command timeout leaves agents running with nobody collecting their work. Four pieces: `wait` returns exit `3` once a run reads abandoned for a grace window (15s, `SPAR_ABANDON_GRACE_SECS`) instead of blocking to its timeout on a run that can never advance; `status --json` carries `orphan_pids`; `spar stop --abandoned` sweeps a project; and SIGINT/SIGTERM to an orchestrator signals its slot groups, then **restores the default disposition and re-raises** so spar still dies on the operator's signal rather than surviving it and reporting a later phase as the run's outcome. Rejected: reaping automatically from any spar invocation — `status` is observe-only by contract, and a human driving a slot through the TUI holds no run lock, so their run reads abandoned too; killing tokens is cheap, killing a live session is not. SIGKILL is uncatchable, so the sweep is the backstop, not the handler | DECIDED |
| O29 | **Worktree reclamation is a sweep, and rejection reaps immediately.** Nothing reclaimed worktrees: `auto_cleanup` defaults false, the auto-clean it gates only fires for `Phase::Done` reached inside `maybe_auto_ship_or_cleanup`, and the normal successful path ends at the **ship gate** instead — so even successful runs accumulated one worktree per slot forever, and `spar cleanup` needed an explicit run id (75 stale worktrees across three projects on the author's box). `spar cleanup --all` now sweeps runs nothing can resume (`done`, `plan_rejected`); `--older-than <dur>` extends it to resumable-at-rest runs (`stopped`/`failed`/`stuck`/`quota`/gates) whose age is the evidence nobody is returning. In-flight runs are never swept whatever their state file says — parking them is `stop`'s job. `spar reject` reaps its run's worktrees inline, since `implement --run` requires an approved plan and a rejected one can never be resumed; artifacts stay. Rejected: flipping `auto_cleanup` to true, which would delete resumable work by default | DECIDED |
| O30 | **The spec overlay carries git-visible files only, and says what it left behind.** `apply_spec_tests_to_impl` walked the test-author worktree unfiltered and byte-copied everything but `.git` into every implementer, so `target/` and `node_modules/` crossed worktrees — gigabytes per slot, on a run still reporting phase `prepare_isolation` (measured: one run blocked >60min in state `D` on `blk_mq_get_tag` with an fd open on `target/debug/.fingerprint/`; impl worktrees reached 54 GB). The overlay now enumerates via `git ls-files --cached --others --exclude-standard`, falling back to the walk only where git lists nothing (the dry-run cwd under an ignored `.spar/`) and saying so when a git *error* forces that fallback, since the walk is the gigabyte copy and must never resume silently. What git ignores therefore no longer crosses — including fixtures a test-author wrote into an ignored path (`.env.test`, an ignored `tests/data/`) — so the overlay **reports** them: the ignored entries (directories collapsed, so `target/` is one line) go to stderr, the event log, the bus, and the implementer's own prompt with the author worktree path and an instruction to copy across what a test needs and to leave build output alone. Silence was the alternative, and it surfaces as an acceptance test failing at runtime for no visible reason, against an implementer explicitly permitted to weaken a test if it documents why. **Rejected: hardlink-seeding build output into slot worktrees** (`cp -al target`, briefly shipped as `[worktree] seed_dirs`). The premise — "cargo replaces outputs rather than rewriting them in place" — is false. Cargo rewrites `target/debug/.fingerprint/*` in place and decides freshness by mtime, so sharing those inodes means one slot's build bumps every other slot's fingerprints; the next slot reports `Fresh`, compiles nothing, and its `target/debug/<bin>` is still the hardlink to the project's binary. Reproduced independently twice: a slot's own change never compiled, exit 0, and the implementer smoke-check plus the authoritative suite then run against a binary without the change. `incremental/` was the wrong exclusion. Two further defects made it unsalvageable in that form: `.cargo-lock` is hardlinked and `flock` is per-inode, so the seed reintroduces exactly the fleet-wide build serialization this entry cites as its reason for rejecting a shared `CARGO_TARGET_DIR`; and `cp -a` implies `-d`, so a symlinked `target/` is copied as a symlink, giving every slot one shared dir and making the `incremental/` cleanup delete outside the worktree. A COW copy (`--reflink=auto`) would give real inodes and is the only version worth revisiting. Also rejected, unchanged: a shared `CARGO_TARGET_DIR` across slots | DECIDED |
| O31 | **A clean exit with work on disk and no artifact is recoverable, not failed.** Slots that backgrounded a long build, spent their remaining turns polling it, and exited 0 without a summary were failed on `missing expected artifact`, throwing away a complete implementation plus its build and re-dispatching from scratch (~45min once). The gate now grants one artifact-only recovery turn (600s, "write this file, run nothing, change nothing else") when the slot's worktree holds commits past `base_commit` or a dirty tree. A worktree with nothing in it still fails — the recovery is for a missing write-up, never for missing work. **Implementer only**, and that restriction is load-bearing: `tester` and `reviewer` slots are pointed at the *implementer's* worktree, so "has work" is true for them whether or not they did anything, and the recovery agent is a fresh session with no memory of the turn it is summarizing. A recovered `suite.md` reading `## Result: pass` would set the authoritative gate green with no suite having run; a recovered `test-contract.md` would carry no `AC-n`, so `parse_contract_criteria` yields nothing and `acceptance_blocks_ship` goes vacuous — a green run with nothing holding it. Those roles keep failing closed into the existing salvage path, where a human sees them. Recovery also gets its own `<slot>.recovery.log` (reusing the slot log would truncate the transcript of the turn that did the work, which is both the operator's only diagnosis and what `salvage_expected_artifact` tails), and is spawned with the same pid marker and liveness beat as any other slot, so `stop --abandoned` can see it and a long recovery cannot let a live holder's path reserves lapse. Paired with the prompt fix: `implementer` and `test_author` now carry the foreground-only rule `tester` already had, since the slots did not know a multi-hour `slot_secs` meant they could just block | DECIDED |
| O32 | **The suite gate reports its own coverage gaps; it never closes them.** The tester writes its own command list, so an implementer that adds a test binary the list does not name gets a green authoritative gate that never compiled the code written to prove the change (observed; a reviewer caught it, the harness could not). spar now diffs added test files against the parsed `## Commands` and appends a `## Coverage warning` to `suite.md` (plus an event and a bus broadcast) — into the reviewers' input, where judgment lives. It does **not** fold the missing targets into the command list: a harness that rewrites its own gate is not a gate. Suppressed unless the commands are *selective*, because a command that runs the project default compiles everything and warning there would fire every run and train the operator to ignore it. Selective means an explicit target flag (`--test`, `--bin`, `--lib`, `mod::case`) or a named test *file* argument. Deliberately **not** selective: `-p`/`--package` (`cargo test -p foo` builds every test target in `foo`, including one added this round), directory arguments, and anything past a pipe or `&&`. An earlier form counted any token containing `/` and so warned on `pytest tests/`, `go test ./...` and `./scripts/test.sh` — the canonical full-suite command of three ecosystems, i.e. exactly the false-positive class this suppression exists to prevent | DECIDED |
| O33 | **`roles`, not `providers`, is the answer to "what is running this".** `state.providers` is the run's pool; printing it at launch and in `status` showed operators providers they had deliberately excluded by role. Launch lines and `status` (human + `--json`) now report the resolved `role=provider` assignment read off the dispatched slots, reviewers joined with `+`; the plan launch line moved after slot resolution to have something to report. `providers` stays in the JSON as the pool. Same reasoning applied to `cleanup --all`, which now prints each **spared** run with a reason (resumable / below `--older-than` / in flight) instead of "nothing to sweep" beside gigabytes of finished worktrees | DECIDED |
| O34 | **Merged is evidence, and it reclaims on its own.** O29 accepted only *age* as evidence that a resumable-at-rest run was garbage, so a run that shipped and landed still had to out-wait `--older-than` while its worktrees held tens of gigabytes (124 GB across a handful of runs, on a box where tmpfs and page cache are already contended). Containment in the run's own `base_ref` is stronger evidence than age — the work is in the base branch — so `cleanup --all --merged` reaps at-rest runs whose every slot branch is an ancestor of their base, and `[worktree] auto_cleanup_merged` (default **true**) runs that same sweep automatically just before a launch cuts new slot worktrees, reporting what it reclaimed and never failing the run. Default-on is the reverse of O29's call on `auto_cleanup` and deliberately so: `auto_cleanup` deletes resumable work off a *phase check*, this deletes only what git says is already merged. **Ancestry alone is not the test.** A slot that wrote code and never committed leaves its branch sitting on the base, which is trivially an ancestor, so ancestry by itself reports "fully landed" for exactly the work most at risk — and `cleanup_run` removes worktrees with `--force`. Reproduced: a run parked at the plan gate with uncommitted acceptance tests was reaped by an unrelated `spar plan`, taking the tests with it and leaving the run unresumable. So a worktree with **uncommitted changes** vetoes the verdict, failing closed when git cannot answer. Two further guards on the sweep: it skips runs whose orchestrator is alive (the phase on disk is a snapshot, and a resuming process is briefly between load and save — reaping there also terminates that run's agents, since `cleanup_run` kills processes whose cwd is inside), and it skips runs with no worktree left on disk (records outlive their worktrees, so without it every launch re-ran `worktree remove` / `branch -D` / `prune` and re-reported reclaiming what it already had). In-flight runs are untouchable; `at_rest` is necessary, not sufficient. Deliberate limitation: containment is **ancestry**, so a **squash-merged** branch reads as unmerged and needs `--older-than` or an explicit run id — the safe direction to be wrong in, since the alternative (patch-id matching) deletes work that only looks landed. Base resolves local-first, then `origin/<ref>` | DECIDED |
| O35 | **`cli:muse` adapter, Meta-account-native, and it sums its own subagents.** New native-cli adapter (`muse exec --json --user-input-auto-resolve --yolo [--model …] --prompt-file <path>`). It exists because spar could only reach the muse-spark family through `cli:opencode@meta/…` at OpenRouter list price (1.25 in / 4.25 out per M), while the muse CLI reaches the same family on Meta's own account, where the contributor model bills 0.10 / 0.20: same tier of model, ~12x cheaper in, ~21x cheaper out, and no `OPENROUTER_API_KEY` to export. `--user-input-auto-resolve` is not optional for spar's spawn model: slots run detached with null stdin, so a `request_user_input` call otherwise hangs the slot to its wall-clock timeout. `--prompt-file` is preferred over the positional prompt the codex/opencode adapters use, since spar fills `prompt_file` at every slot call site anyway. **No `--model` is emitted when nothing selects one**, so muse's `settings.json` picks the tier: the contributor discount is paid for with "your content may be used for product improvement", which is a per-box decision, not something spar should bake into every repo it touches (pin `cli:muse@muse-spark-1.2` where it matters). `model_select` maps `meta/*` to `cli:muse` ahead of `api:meta`. **Token accounting deliberately diverges.** muse reports zero usage on stdout, so `providers::muse_telemetry` reads the session log (`${XDG_DATA_HOME:-~/.local/share}/muse/sessions/YYYY/MM/DD/<session-id>/`) after the slot exits, keyed by the session id the stream parser captures, and sums the `goal_usage_attribution` records across the session **and every `subagent/*/session.jsonl` under it** — a trivial one-file task fanned out to six subagent sessions carrying ~29k of its ~76k input, spend the opencode and codex adapters would never see. Input is **summed** across model calls, not maxed as in `absorb_usage`: each record is one billed call that re-sends the context, so the sum is what Meta charges while the `max` convention reports final context size. The two numbers are therefore not comparable across adapters, which is the accepted cost of the route existing to be measured. v1 is agy-tier (`DeliveryStrategy::None` / `PresenceSource::None`); muse ships `session-message send\|serve` over a unix socket, a real turn-boundary channel, reserved and not wired | DECIDED |
| O36 | **Archiving is presentation; cleanup is lifecycle.** `cleanup` reclaims worktrees and deliberately keeps `.spar/runs/<id>` — that is where `plan.md`, `test-contract.md`, the reviews, `suite.md` and `stats.json` live — so nothing ever removed a *record*, and a CLI-driven project accumulated one listing row per run forever (payforge: 69 rows, of which 28 `done` and 4 `plan_rejected`, with **12 runs parked at a gate** buried among them). The answer is a third state between listed-forever and deleted: `archived_at` on the run, hidden from `status` and the TUI rail, shown by `status --archived`, reversible with `archive --undo`, and deleting nothing. Filtering happens at the *listing surfaces*, never inside `list_runs` / `list_all_runs`, because resolving a run by id has to keep reaching archived runs — archiving hides a run from browsing, it does not retire the id. Three rules keep it safe on by default (`auto_archive_after = "14d"`): only `done` / `plan_rejected` auto-archive, never a **gate** (hiding the runs that want a human is the failure this feature exists to prevent) and never the ambiguous `stopped`/`failed`/`stuck`/`quota`; a run stays archived only while its phase stays *archivable* (`done`/`plan_rejected`), so resuming, re-approving or parking at a gate all restore it — keyed off the archivable set rather than `at_rest`, because `spar approve` accepts a `plan_rejected` run and moves it to `PlanApproved`, which is inside `at_rest`, leaving an approved run hidden while it waited for `spar implement`; the manual-archive refusal likewise gets its own `archivable_by_hand` predicate so a pending change to the sweep's notion of rest cannot silently alter what may be archived; and the sweep fires at **launch** only, never from `status`, keeping read commands observe-only (same rule as `stop --abandoned`). `save()` does not touch `updated_at`, so archiving preserves a run's true age and `cleanup --older-than` still sees it | DECIDED |

| O37 | **The unsaved-work veto belongs at the deletion, not at an evidence path; and build cache is reclaimed separately from worktrees.** O34 put the dirty-tree check inside `merged_into_base`, which guards only the `--merged` path — so `cleanup --all --older-than` walked straight past it and force-removed worktrees holding uncommitted agent work on payforge. Guarding one evidence path leaves the next one unguarded, so the check moves to `cleanup_run`, the single choke point every caller (`cleanup_sweep`, `cleanup_one`, `sweep_merged`, `plan::reject`, the auto-cleanup in implement/peer/review) already funnels through, reported via the `skipped` field it already had. It vetoes **unmerged commits as well as dirty trees**: `remove_worktree` runs `git branch -D`, so a commit the base does not contain is as gone as an unsaved edit — the biddesk survey found 16 such commits across three parked runs, all of which the age path would have taken. Dry-run cwds under `.spar/` are exempt (git cannot answer for a scratch dir, and fail-closed there would refuse every dry-run cleanup). `--force` exists for an explicit run id only; `--all --force` is refused, because a sweep chooses its own targets and pairing that with an override is how one command destroys a project. Separately, **gates leave the age path entirely** (`age_sweepable` = `resumable_at_rest` minus gates): a run at `awaiting_plan_approval` is idle because the *operator* was busy, so age there is close to anti-evidence. Split rather than edited, because `resumable_at_rest` also feeds `at_rest`, `sweep_merged` and the skip-reason messaging. Finally `spar reclaim` / `auto_reclaim` (default on) deletes `target/` + `node_modules` inside finished runs' worktrees, keeping worktree, branch, commits and uncommitted changes — deliberately a separate command from `cleanup`, since it needs none of cleanup's evidence and must not become a back door into an autonomous sweep. Cache detection is a **disjunction** (`CACHEDIR.TAG` or `.rustc_info.json` or `debug/` or `release/`): cargo writes the tag late, and a build interrupted before that leaves a multi-GB tree a tag-only test strands forever (measured: 22 GB). Live-process cwds are snapshotted once per pass, not re-walked per candidate. Motivation measured 2026-08-17: 457 GB of 587 GB under one projects dir was build output, 26 of 32 slot worktrees belonged to already-terminal runs, and the largest object on the machine was a stopped run's 72.7 GB target dir | DECIDED |
| O38 | `--detach` must actually detach: `setsid` / `process_group(0)`, plus a startup handshake where the parent confirms the child holds the `RunLock` before reporting success. Today it prints "detached" and a child that dies at startup is invisible for the whole 15s `abandon_grace` (`src/executor.rs:1874`). Corrects the false claim at `skills/core.md:280` | DECIDED |
| O39 | Lifecycle notification fires from the `RunState::save` phase-transition choke point (`src/state.rs:441-454`), not per call site, into the existing B1 sink. Gate, stuck, quota, abandoned and terminal failure push; silence means healthy | DECIDED |
| O45 | **A run is a unit of work, not an invocation.** One run id covers an issue (or a bundle of them) from brief to draft PR, and everything spawned for it — planner, critic, test author, implementer, tester, reviewers, a replan after rejection, N fix rounds — is a **round inside that run**, never a run beside it. The invariant already said "one run id threads plan → implement → ship"; nothing enforced it, and `run_from_cli` (`src/workflow/implement.rs:27-32`) offered the bypass outer agents take by default: `--plan <path>` with no `--run` mints a fresh id auto-titled `Implement approved plan from <path>`. Measured on biddesk 2026-08-21: 106 visible runs, of which only 24 are the full pipeline on one id, 35 are implement legs with no planner, 31 are stranded plan legs, and `parent_run` is set on **zero** of them — so ~66 rows are halves of ~33 units of work, burying 9 runs genuinely waiting on the operator. Continuing is therefore the default and minting is explicit: a `--plan` path under `.spar/runs/<id>/artifacts/` resolves to that run, a bare `-t`/`--plan` errors with the candidate runs named, and `--new` is the escape. `plan --run <id>` opens a replan round rather than a second run. **`spar review` keeps its own run** — pointing a review panel at a branch spar did not build is a real use and deserves its own row — and no fifth noun is added: the run *is* the issue (U6) | DECIDED |
| O46 | **Legs that already exist are linked by hand, never inferred.** `parent_run` is on disk and unused; `spar link <run> --to <parent>` sets it and `--undo` clears it. spar does not pair legs by task text — biddesk's 66 legs would have to be matched on prose, and a wrong guess merges two unrelated issues. The TUI folds a linked leg into its parent's row and rolls its attention flag up, so one unit of work is one row; `status --json` keeps listing every run and carries the link, so outer agents lose nothing. Separately `archive --all` gains the hand-archivable phases (`stopped`/`failed`/`stuck`/`quota`), which auto-archiving deliberately never touches (O36): 52 of biddesk's rows are `stopped`, and one-at-a-time is not a cleanup path | DECIDED |
| O40 | **Per-project daemon** supervises runs: restarts a dead orchestrator, pushes lifecycle notifications, computes abandonment (extends Z2), holds the cross-run queue. It never approves, confirms, merges, sweeps `cleanup`, or picks a fleet. Gates stay human | DECIDED |
| O41 | Task intake is a **written brief on disk**: `spar plan --spec <file>` / stdin, stored at `.spar/briefs/<slug>.md`; `spar brief <id>` re-hydrates a fresh session in one call; `spar resume <id>` recovers a stopped or abandoned run | DECIDED |
| O42 | **Cross-run concurrency cap**, enforced by the daemon, keyed on the quota bucket (X8 `storage_key()`), because `max_agents` (`src/config.rs:11`) is per-run width only. Five of seven standard-fleet seats bucket as `cli:claude`, so three concurrent runs lose three plans and three contracts to one limit | DECIDED |
| O43 | **The acceptance contract is frozen when the round loop is entered, not re-parsed each round; a mid-run edit is surfaced loudly instead of taking effect.** `execute_loop` (`src/workflow/implement.rs`) freezes `test-contract.md` into a `FrozenContract` (body, declared criteria, fingerprint) once, before the round loop starts. Re-parsing every round was rejected: on `d995e566` the Opus reviewer caught the implementer editing both the contract and the acceptance test it was judged against, and the contract lives outside the repo (`.spar/runs/<id>/artifacts/`), invisible to `git status` and to any diff, so re-reading it each round would have made that edit *effective* — converting an audit problem into an escape hatch for the slot the gate exists to bound. Instead, each round re-reads the file from disk and compares it against the frozen body; a mismatch sets `state.contract_modified`, is named as an `Event::info`, broadcast on the bus, printed to stderr, and handed to reviewers as `{{contract_drift_note}}` — never blocking, and spar never rewrites the file. The legitimate amendment path already existed and just needed naming: stop the run, edit `test-contract.md`, re-run `spar implement --run <id>` — `execute_loop` re-reads and re-freezes on every entry. Paired with the declaration-grammar fix (this same change), the phantom-criterion case that motivated wanting live re-parsing cannot recur | DECIDED |
| O44 | **A shipped run's acceptance suite gates one diff, not the repo.** `tests/docs_contract.rs` was run `d995e566`'s frozen, diff-scoped acceptance suite (merged `61b6ba1`): it diffs the working branch against its merge base with `main` and asserts things about that diff (no `src/`/`skills/`/`templates/` touched, changes confined to a fixed deliverable set, a fixed trailing `DECISIONS.md` id). Any branch that touches those paths or appends a `DECISIONS.md` row fails it by construction, including this one. Deleted along with `tests/fixtures/docs-baseline/` rather than edited to pass, since rewriting a frozen acceptance suite so a later change clears it is the anti-pattern this run exists to close. The general rule: a merged run's acceptance suite is disposable once its own PR lands — it is not a permanent repo invariant, and the next run's test author should expect its removal rather than treat it as vandalism | DECIDED |
| O47 | **Slot prompts tell agents to keep long build/test output out of the transcript.** Measured across 197 runs, token cost is roughly quadratic in tool calls — every call re-sends the whole accumulated transcript, and context-per-tool-call rose from 74k at 25–50 tools to 272k at 200+ — so a full `cargo build` or suite dump read once at turn 30 is re-billed on every turn after it. spar cannot prune a vendor CLI's transcript, so the only lever is the prompt: `implementer`, `tester`, `test_author`, `reviewer_adversarial`, `reconciler` and `peer_half` now say to redirect verbose output to a log and read back the tail, or the grepped failure. Three things it deliberately is **not**: (a) not permission to background — each rule restates "volume, not scheduling: still foreground, still blocking", because the same templates carry the hard-won no-`&`/`nohup`/`disown` rule and a slot that reads this as "start it and poll" exits without its artifact (O31); (b) not a blind `tail -5` — every rule requires reading enough of the log to actually diagnose a failure, since trading tokens for a missed diagnosis defeats the purpose; (c) not enforced — spar injects no redirect and wraps no command, because the slot chooses its own commands and a spar-side pipe would change what TTY-shaped providers see. Log names are **slot-scoped** (`build-{{slot_id}}.log`, `suite-{{slot_id}}.log`, `test-build-{{slot_id}}.log`, `check-{{slot_id}}.log`) and live in `{{artifacts_dir}}` — run-scoped, outside the worktree, so they never land in a diff or a commit, and never `/tmp`. The slot suffix is not decoration: `artifacts_dir` is shared by every slot in the run, and arena spawns N concurrent implementers that all render the same `implementer` template (`src/workflow/arena.rs:35`, `:123`, `:181`), as do its concurrent reviewers (`:376-389`) — one fixed `build.log` would interleave and a slot would `tail` another slot's compiler output and chase a phantom failure. It is the same reason `summary-{{slot_id}}.md` and `review-{{slot_id}}.md` already carry the suffix | DECIDED |
| O48 | **`context_tokens` is a gauge, `billed_tokens` is a meter, and one number was doing both jobs badly.** `StreamStats::context_tokens` was `input + cache_read + output` for every adapter, which is a running total dressed as a window: on claude the terminal `result` carries the invocation's *cumulative* cache read (54,623 on a two-message probe), so the TUI's 80k/150k gauge read every claude slot as permanently over threshold, and on muse it double-counted outright because `cached_tokens` there is a **subset of `input_tokens`**, not claude's disjoint sibling (verified: 40,258 provider records across the local corpus, zero with `cached > input`, mean ratio 0.93; run 2736a545 reported 67.3M context against a real billed total of 35.0M). The verification behind all of this is **session-scoped**, which bounds what it can prove: it reconciles a slot against the provider's own ledger *for the session spar named*, so spend booked to a child session it never sees is invisible to the check by construction (opencode `task` subagents; `roadmap/BACKLOG.md`). The field splits. **`context_tokens` is the peak prompt footprint of a single request** (`input + cache_read + cache_write`, maxed over per-request usage records; for muse the peak per-record `input_tokens`, which already contains the cached part), never accumulated, so it stays comparable to a model's window. **`billed_tokens` is cumulative spend** and is exactly `input + cache_read + cache_write + output` as those fields are written to the sidecar, with reasoning folded into `output` because it bills at output rates and no provider includes it there (verified against opencode's own `tokens.total`, which is `input + output + reasoning + cache.read + cache.write` per step). **Two adapters report the cached prompt as a *slice of* `input_tokens` rather than a sibling of it** and both are normalized to the Anthropic shape on the way into the sidecar, `input_tokens` holding the uncached remainder, so the identity holds without a per-adapter carve-out: muse's `cached_tokens` (the subset this row already names) and **codex's `cached_input_tokens`**, which an earlier cut of this change folded into the same slot as claude's disjoint `cache_read_input_tokens` and thereby billed twice. codex's own ledger settles it: across 105 `token_count` records in `~/.codex/sessions`, `total_tokens == input_tokens + output_tokens` in all 105 and `cached_input_tokens > input_tokens` in none, so a real record of input 39,189 / cached 39,185 / output 117 bills 39,306 and the summing form computed 78,491, 1.997x. Latent when it was written (no codex dispatch in the corpus) and wrong the first time a `cli:codex` seat ran, since `nudge.rs` fires the soft budget on `billed_tokens`. Adapters that report only an invocation total (codex's `turn.completed`) have no per-request record to peak over, so their total stands in for the gauge; `cli:agy` is a second gauge exception beside grok, reporting the *latest* call's prompt rather than the largest, because its statusline sink emits one snapshot and keeps no history (a real window reading, just not a maximum); a terminal record **supersedes** the per-call accumulation rather than adding to it, which is what makes claude exact (its `result` equals the sum of its per-message records, while its per-message `output_tokens` is a streaming understatement and its `assistant` events repeat one message's usage once per content block). A token budget is enforced on `billed_tokens` and never on `context_tokens`. Three parsing bugs fell out of the same audit and are fixed with it: opencode's `step_finish` fields are **per-step deltas** and were being `max`'d, under-reporting a real 20-step session by 11.9x (2,730,823 billed against 228,839, replayed from `opencode.db` through the parser) — summing is safe only because the part dedupe runs first, and the dedupe earns its place on `PartUpdated` **republishes** of one `part.id` (every consumer in opencode's own bundle upserts parts by `messageID:partID`), not on a dash/underscore double-emit: the json emitter writes five hardcoded underscore literals and the dash spelling appears only on `part.type`, so the `-`-to-`_` fold is defensive and the earlier comment claiming otherwise was wrong; the stderr reader shares one `StreamStats` with stdout and copied its own never-parsed zero counters over the real ones, so any slot whose last line was stderr reported `"tools": 0` (biddesk 2736a545: 191 tool lines, zero recorded); and counters were persisted only alongside a log append, so opencode's terminal `step_finish`, which prints nothing, never reached `stats.json` at all. muse's tool count is now also recovered from the `usage_family == "tool"` records in its **main** session log (1:1 with the stdout count: 112/112, 114/114), and billed records are selected by `quantity.reported == true` rather than a family allowlist, which was silently dropping billed `compaction` records. **`cli:grok` is the one adapter this does not settle, and the table must say so.** grok runs with `--output-format streaming-json`, which its own help calls the native **ACP** session-update stream, not the Anthropic wire format (`streaming-messages-json`, which spar does not request). No grok slot log on the box carries a `· session`, `· done` or `· turn` marker, so grok reaches neither claude's `result` arm nor codex's `turn.completed` and never receives a terminal record, so its numbers come only from the per-request arm. Checked against grok's own session store (`~/.grok/sessions/<cwd>/<id>/updates.jsonl`, whose `turn_completed` update is ground truth) for biddesk run 92ae513a: `cache_read` was exact (2,853,504) and `input_tokens` exact as the uncached remainder (124,866 = 2,978,370 - 2,853,504), since grok emits both cumulatively and `max` lands on the final value, but `output_tokens` read 61,292 against a real 30,646, a cumulative figure counted twice. Removing the duplicate `absorb_usage` calls (this change) is the likely fix and is **unverified**, grok being out of quota and deliberately not probed. Left as a separate defect rather than rewritten blind: grok's `context_tokens` is a cumulative total wearing a peak's name (its own `modelCalls: 31` says the real window is far smaller), its `model` is never captured, its `tool_errors` is structurally always zero, and its tool names never resolve, though the tool count is exact (83 against 83 `tool_call` updates). Treat grok's `billed_tokens` as approximate until a probe settles it. **Amends O35**, which documented muse's sum-vs-max divergence but not the subset relationship that was the actual trap | DECIDED |
| O49 | **A slot's terminal record is a marker on disk, not a line in `state.json`, and the paths that outlive an orchestrator persist the reconciliation.** `status` was only ever advanced by the in-process orchestrator on the same code path that had just waited on the child, so `running` was durably saved at dispatch and a terminal status only if the orchestrator survived the wait: asymmetric by construction. Measured across the local corpus: **110 of 851 slots are permanently `running`**, 98 of them in runs `spar stop` reaped (`reap_run` SIGTERMs the orchestrator, which dies inside `run_captured`, and `stop_one` then set `phase = Stopped` without touching `slots[]`), 12 from a crashed or OOM-killed orchestrator. `load_for_display`'s marker reconciliation was sound and failed only on **coverage**: 86 of the 110 have no terminal marker at all, because markers were written after the wait, on the same dead path. Three changes, in that order of leverage. **Markers are written the moment the child is reaped**, before the artifact and recovery gates that can still downgrade a clean exit (`executor::execute_prepared`, `executor::run_headless`), and carry a JSON body (`ok`/`round`/`pid`/`exit_code`/`signal`/`reason`) so a spar-authored verdict can be told from one an agent wrote by hand into the same path. `markers::terminal_marker` still accepts either, deliberately, since an agent's marker is the only terminal record a slot has when nobody came back for it (real case: `6679c92d`'s `impl.done` holds a git SHA the agent wrote). **The write stays purely additive**, which is not a detail: an earlier cut of this change deleted the losing marker, and since six templates tell the agent to write its own `<slot>.failed` with a reason while a CLI agent exits 0 whenever its turn ends, "agent declares failure, process exits clean" is the normal case. Deleting `.failed` on a clean exit inverted the `.failed`-wins precedence on exactly the path this hardens, silently reconciling a self-declared failure to `done`; leaving both is what that precedence is for, and it also removes any instant with no terminal marker at all. **`RunState::reconcile_and_save` persists** the marker pass and additionally demotes a still-`running` slot to `failed` when nothing owns the run and no *trusted* live pid stands behind the slot, trusted meaning a `PidToken` carrying a `/proc` start-time, since a start-time-less token cannot tell its process from a later reuse of the pid. (`live_slot_pids` deliberately trusts a bare pid instead: there, missing an orphan leaves it burning tokens, so the two functions trade the same uncertainty in opposite directions and both say so.) It is called from `stop_one` / `stop_one_quiet`, `RunLock::acquire`'s reclaim branch (the one moment spar knows an orchestrator died), the top of `execute_loop` on resume, and `reconcile-state`. **Who owns the run is a `RunOwner` the caller asserts, never re-derived inside the reconcile**, and getting that wrong made the first cut a no-op for the majority population: `RunLock::acquire` stamps its *own live pid* into the lock before it can reconcile, and `execute_loop` already holds the lock, so a reconcile that asked `orchestrator_alive` found itself, concluded the run was owned, and skipped the demotion. Only the two `stop` paths actually demoted, while the reclaim, the one place that settles a crash nobody is going to `stop`, ran the marker pass alone and left all 86 markerless slots exactly as they were. The `stop` paths turned out to need the same treatment for a different reason: a process spar has just SIGKILLed is a **zombie** until its own parent reaps it, and a zombie answers `kill(pid, 0)` and keeps its `/proc` start-time, so `PidToken::alive()` reads the orchestrator spar itself killed as a live owner. Observing after the reap therefore skipped the demotion whenever `spar stop` ran while something still held the orchestrator as a child, which is the ordinary case for an outer agent. `reap_run` establishes `Nobody` (SIGKILL is unblockable, and reaping the driver is the entire point of `stop`), so `stop` asserts it too. Only `reconcile-state`, which holds no lock and killed nothing, observes. The demoted slot's `error` is likewise the caller's: `orchestrator died mid-dispatch` for a crash, `halted by operator (spar stop)` when `spar stop` reaped a run that still had a live orchestrator, because telling an operator their own halt was a crash is a false report on their own run. **`load_for_display` stays read-only**: a read command must never be the thing that rewrites a run, `load_for_display_reconciles_without_rewriting_state` pins it, and `wait`/`stop` now read through it so their `--json` stops serializing stale slots. Two corollaries. Dispatch **resets the per-dispatch fields** (`exit_code`, `signal`, `pid`, `error`, `usage`) alongside `status`, which is why 60 of the 110 showed the impossible `running` + `exit_code: 0`, and why `6679c92d` has slots at `done, exit_code: 0, error: "exit 143"`, a stale error on a slot that looks final, which is the worse half of the same bug. And `apply_parallel_outcome` **saves per join** instead of once after all of them, so a batch that loses its orchestrator mid-join no longer discards the verdicts already collected. `spar reconcile-state [--all] [--apply]` backfills existing runs, dry-run by default; it **skips any run with a live orchestrator** (it decides from a snapshot and writes later, so applying to a run in flight can stamp over a verdict the orchestrator recorded in between, and `--all --apply` is precisely the invocation most likely to catch one), and it patches **only** `slots[].status` and `slots[].error` into the existing document and is deliberately **not** part of `spar cleanup`, which force-removes worktrees: a worktree is often the only copy of a slot's work, and this project has already destroyed real work to a sweep wearing a housekeeping name. It also does **not** backfill `usage` from the `logs/*.stats.json` sidecars: 22 of the 110 are un-enriched muse zeros (`enrich_muse_stats` runs only after the child is waited on), so a backfill would write wrong numbers into the accounting record O48 just made trustworthy. Note the table's O45/O46 rows sit above O40-O44 rather than in numeric order; O47 is taken on a parallel branch and O48 in this one, so this row is O49 | DECIDED |
| O50 | **A slot is never killed for spending tokens; it is nudged, repeatedly, and the wall clock splits into a soft budget and a far higher hard ceiling.** The measurement that settles it: of the 21 dispatches in the local corpus that billed over 100M tokens, **18 exited `0`**. The expensive tail is expensive-but-working slots, not runaways, so a token cap would destroy finished implementations and force a re-dispatch, which is the most costly thing spar does. `[budget]` therefore carries a per-role **soft** budget on one dispatch's `billed_tokens` (O48's meter, never `context_tokens`, never `slot.usage`), sized at each role's measured p90 across 1794 dispatches: implementer 60M, test_author 20M, reviewer 12M, planner 8M, plan_critic 6M, tester 6M. One global number is wrong because the distributions differ by more than 10x, and crossing a budget only tells the slot to write its artifact now and say what it did not reach, renudging every `nudge_fraction` (0.2) of the budget past it, so a slot at 2x has been told five times rather than once. **`timeouts.slot_secs` is now soft too**, a deliberate behaviour change to an existing knob: past it the slot is asked every `nudge_every_secs` (600) whether it is still making progress, and the only kill is `hard_ceiling_multiple` (3.0), a backstop against a hung process rather than a second budget. The multiple is a multiple and not an absolute because roles draw different soft budgets (`suite.timeout_secs` for tester, `spec.timeout_secs` for test_author, `review_secs` for reviewer), and it is generous because the duration data is censored at exactly the number under review: every observed maximum is the project's own kill (180.0m under payforge's 10800s, 90.0m under biddesk's 5400s). **The `slot_secs` default moves 1800 → 5400**, resolving a real contradiction: `templates/implementer.md` promised agents "hours, not minutes" while the default killed at 30, and measured over 1869 real dispatches **38.7% of implementer dispatches run longer than 30 minutes** (p50 22.9m, p90 76.8m). The default was fixed rather than the template because the template was operationally right and every project on this box had already overridden the default upward; the template's magnitude claim was reworded anyway, since 90 minutes is not "hours" and the soft/ceiling split is what actually makes blocking on a long build free. At 5400 the threshold selects the slowest 6.6% of implementers, which is what a soft budget is for. **Delivery is the hard half.** Text typed at a busy CLI agent queues unsubmitted in its input box, so a nudge only lands at a turn boundary. claude had `StopHookInject` and grok `NativeQueue`; opencode, muse and codex had `None`, which meant the nudge reached everything except the implementer, the role that is 63% of all token spend and every one of the top 21 dispatches. `DeliveryStrategy::PollFile` replaces `None` on those three: spar writes to `.spar/runs/<id>/logs/nudges-<slot>.md` and the role prompt tells the agent to read it before starting any new major step, which is the only moment a nudge is actionable, so it needs no polling loop. The file is slot-scoped because `artifacts_dir` is shared and arena spawns N concurrent implementers off one template. Adapters with a real channel keep it and the orchestrator never learns which it got: it hands `providers::delivery::nudge` the strategy from the adapter and nothing else. **muse's live blindness is fixed rather than worked around.** `muse_telemetry::enrich` runs only after the child is waited on, so a live muse slot's sidecar reads zero for its entire life and a token nudge could never fire for the implementer. muse's session log was checked, not assumed: a real 190-second dispatch was watched from outside and accumulated 92 billed `goal_usage_attribution` records and 1.47M billed tokens monotonically while the process was still up, so `LiveUsage` tails it incrementally (byte offsets, stopping at the last newline, rescanning `subagent/` each poll) from the same `run_captured` tick the liveness heartbeat uses. One O48 gap surfaced doing it: counters were merged into the sidecar only alongside a log append, so opencode's `step_finish` (a whole model call's tokens, printing nothing) left the sidecar behind the stream; the merge is now per line. **Live token visibility is not uniform and the docs say so**: opencode and muse are exact live, claude's per-request arms are `max`ed until its terminal `result` so a live reading is a lower bound and its nudge fires late but never early, codex reports usage only at `turn.completed` and is **time-nudged only**, and grok's output is 2x over (tracked in `roadmap/BACKLOG.md`). **Ceiling headroom per role is checked in O51, which extends this row**: the 3.0 multiple is not uniformly safe, and `spec.timeout_secs` had to move with it. Both nudges ask for one artifact shape (done / not reached / stuck on) and the role templates carry the same shape, so a summary written under pressure has a defined form. The nudge names **every** file the slot owes, not one: `WatchSpec::artifacts` is a list, because a role that owes a summary *and* a carry-forward brief and is told to write "your artifact" writes the first and stops, disabling the carry-forward loop in exactly the case it exists for. Every nudge and every ceiling kill is an `Info` event tagged with its slot; a ceiling kill records `error: "hard ceiling: …"` and `"ceiling_kill": true` in `status --json` so it is distinguishable from a crash without parsing prose. Exit codes are untouched | DECIDED |
| O51 | **Size a soft budget just above the role's measured p90; size a ceiling against the role's observed *max*, never by trusting the multiple.** O50 set `hard_ceiling_multiple = 3.0` and left each role's soft budget where it was, which quietly assumed 3x is enough headroom for every role. Checked rather than assumed, per role, observed max against the new ceiling: **test_author 89.6m against a 5400s (90.0m) ceiling, +0m** · implementer 180.0m against 16200s, +90m · tester 180.7m against 21600s, +179m · reviewer 185.4m against 16200s, +85m · planner 21.5m against 16200s, +249m. test_author was the one role with no margin at all, and the worst to lose: its artifact is the frozen acceptance contract that gates the run, so a kill there wastes the plan and the critique above it too. **`spec.timeout_secs` therefore moves 1800 → 3600**, by the same method that took `slot_secs` from 1800 to 5400: the soft threshold sits just over the role's p90 (test_author 45.8m across 106 dispatches), so it selects roughly the slowest 5% for nudging, and the ceiling lands at 10800s with ~90 minutes clear of anything observed. The trap this exposes is structural, not arithmetic: **`timeout_for_role` does not route every role to `timeouts.slot_secs`** — `Tester` draws `suite.timeout_secs`, `TestAuthor` draws `spec.timeout_secs`, `Reviewer` draws `review_secs` — so raising `slot_secs` looks like it covered the fleet while two roles sit on untouched numbers, and `hard_ceiling_multiple` then multiplies whichever knob the role actually drew. `spec.timeout_secs` was audited to exactly one behavioural consumer (`executor.rs` `timeout_for_role`; the other `cfg.spec` reads are `spec.enabled`, which only decides whether the test_author slot exists), so the change moves that role's soft threshold and its ceiling and nothing else. `each_role_draws_its_own_soft_budget_and_the_ceiling_follows_it` pins the routing with a distinct value per knob, so a role reading the wrong one cannot pass by coincidence. **The method is the part worth keeping**: p90 for the soft threshold, observed max for the ceiling, and remember the duration corpus is censored at each project's own kill (implementer 180.0m, tester 180.7m, plan_critic exactly 90.0m are all project kills wearing the costume of a distribution), so an observed max is a floor on what the role may legitimately need, not a bound. **Extends O50.** Note on numbering: O45/O46 sit out of order above, O47 was taken on a parallel branch, O48 and O49 on this one, and O50 is this branch's nudge decision, so this row is O51 | DECIDED |
| O52 | **A round is bounded, and it carries a compact brief to the next one.** Measured over 197 real runs: median cost by fix rounds is 10.8M (n=126) / 70.9M (49) / 118.3M (13) / 108.9M (7) / 283.0M (2), so **one fix round is a 6.6x median run**, runs with at least one are 65% of the corpus, and slots dispatched more than three times are 53% of it. `max_fix_rounds` bounded the *fix* loop at 3 but `state.round` was unbounded, and the corpus contains runs that reached rounds 9, 13, 17, 19, 20, 26 and 34. **(a) Ceiling:** `[rounds] max` (default 8) is the highest round a run may reach; past it `implement` parks the run at the new `awaiting_round_extension` phase — a **human gate, exit 2**, not `stuck`/3, because nothing is broken: the run has spent the re-dispatch budget it may spend on its own, and only a human can authorise more. `--max-rounds <N>` lifts it and sticks on the run; the ceiling is frozen at creation like the rest of a run's config (O27) and is checked *ahead of* the rotate-implementer / widen-reviewers ladder, since both of those buy more rounds. 8 is defended as plan(1) + implement(1) + the full 3-fix budget = round 5, which covers 95% of the corpus (188 of 197 runs used <=3 fix rounds), plus headroom for one replan or one rotation. `0` disables it. **(b) Carry-forward:** each round the implementer writes `artifacts/carry-forward-<slot>.md` and the next round's implementer prompt is seeded with it via `{{carry_forward_section}}`, because a round is a *cold* re-dispatch — truncated log, empty context, the repo re-derived from scratch before any new work — and nothing carried forward except `plan.md` and `test-contract.md`, which the slot already had. The brief is bounded (`[rounds] carry_forward_chars`, default 4000, truncated with a visible marker), orchestrator-known blockers lead so slot prose cannot squeeze them out, it is **consumed on read** (a stale brief describing a tree that has since moved is worse than silence), and it is scoped **per slot** because `artifacts_dir` is shared and arena runs N implementers at once while rounds re-dispatch the same slot id. The blocker list is rebuilt from the on-disk `review-*.md` every time the loop starts, not carried in memory, or the round an operator buys at the ceiling gate — a fresh process, the most expensive round of the run — would run blind. It carries *context, never verdicts*: no reviewer and no gate reads it, so it cannot argue a failed `AC-n` past the acceptance gate (O43 untouched). Fix rounds also learn *which* `AC-n` blocked them for the first time — `acceptance_block_reason` existed and went only to the bus, leaving the fix round's implementer prompt byte-identical to round 1's. **Session resume was considered and rejected:** it would carry the whole failed attempt's transcript, so round N+1 starts its context climb from a huge base; token cost is roughly quadratic in tool calls within a dispatch (measured 74k context-per-tool at 25-50 tools, 272k at 200+), so a low floor plus a compact brief beats a warm cache with a huge one. **Ordering against the stuck ladder:** the rotate → widen → `stuck` ladder is resolved *before* the ceiling, so exit 3 ("cannot be fixed") always outranks exit 2 ("costs too much"); the reverse order let an already-exhausted ladder present as a question. A lift preserves `fix_rounds` / `rotated_implementer` / `widened_reviewers` (`prepare_implement_slots` used to reset them on every entry), so an outer agent lifting in a loop still terminates at `stuck`. The full ladder costs ~13 rounds, so the default ceiling deliberately asks before it completes. **`--max-rounds` refuses `0`:** turning the bound off is `[rounds] max = 0` in `spar.toml`, a project decision, not a flag typed at a gate. **The gate is not a contract re-freeze door:** O43 made re-freezing a deliberate human act and this gate makes it routine and orchestrator-prompted, so a re-entry whose incoming state carries `contract_modified` refuses (exit 1) rather than adopt an edit spar watched happen under the slot the contract bounds — the escape is `--accept-contract`, and every re-freeze is now announced on stderr rather than only in `events.jsonl`. **Id note:** O45/O46 sit out of numeric order above and O47-O51 were claimed on parallel branches that had not landed when this was written, so this took O52; check for collisions before claiming O53 | DECIDED |
| O53 | **The test-spec overlay carries the author's *uncommitted* work only; committed author work is the merge's job, and a failed merge fails the dispatch.** `apply_spec_tests_to_impl` overlaid every git-visible file in the test-author worktree, sourced from `git ls-files --cached --others`. That is not "the tests the author wrote", it is the whole repository at the author branch's revision, so every dispatch after the first reverted the implementer's own source to that revision. Measured in live run `571659b3`: the overlay copied **3202** files, **486** of them production source under `biddesk/src/`, while the author had **0** modified and **0** untracked files — 3202 files stomped to deliver zero. `PipelineSuite` vanished from `models/task.rs` and 398 lines from a download handler, both byte-identical to `git show <author-tip>:<path>`; run `e663edaf` lost an entire fix round the same way. It reproduced from a clean tree at HEAD, so "commit before dispatch" was never a workaround. The overlay now lists only what a merge cannot deliver (`git diff --name-only HEAD` ∪ `git ls-files --others --exclude-standard`). Two consequences taken deliberately: (1) the narrow scope **never falls back to a wider list** — an empty answer is the normal answer for an author who committed everything, and a git that errored gets a hard error rather than the full-tree walk, because that walk *is* the data loss; the wide scope and its walk survive only for dry-run and `.spar/`-hosted cwds, which have no shared history to merge. (2) the merge is now load-bearing rather than an optimisation, so `merge_spec_branch` **fails the dispatch** instead of aborting quietly — a swallowed conflict now means the acceptance tests never arrive at all, and an implementer graded against tests it cannot see is worse than a dispatch that stops and says why | DECIDED |
| O54 | **A slot that dies mid-dispatch from a rate limit parks the run at `Phase::Quota` (exit `4`), not `Failed` (exit `1`), that park is resumable, and the routing decision is a narrower signal than the pause.** Found dogfooding on 2026-09-04, runs `bf7770ae`/`abd35a54`. Round 1 of this fix routed `quota_hit` straight off `detect_and_pause_quota`'s existing scrape and wired it into `workflow/implement.rs`'s `fail` (implementer dispatch) only. Round 2, driven by review: (1) **the discriminator was unsafe.** `detect_and_pause_quota` reused `QuotaStore::scrape_log_hint`'s broad needles (`"quota"`, `"429"`, `"capacity"`, single words) to decide routing, not just the pause; driven against realistic non-quota failures (a panic whose line number contains `429`, an implementer editing this very module's `pause_quota`, a test named around "rate limiter", a capacity-planning doc edit) it returned `quota_hit = true` on all four, which would have parked a real defect on the quota gate — worse than the bug being fixed, and named as such in the task. Fixed by splitting the two: `scrape_log_hint` still drives the cheap, auto-recovering pause unconditionally, but the `bool` returned to callers (and thus `SlotState.quota_hit`, and thus the phase routing) now comes only from `scrape_claude_rate_limits` (Claude's structured/stated-reset shapes) or the new `QuotaStore::scrape_strong_quota_signal` — line-scoped, requires a limit phrase *and* a rejection word (`rejected`/`exceeded`/`reached`) together, or an exact phrase like `"out of credits"`/`"quota exceeded"`/`"429" + "too many requests"`. (2) **routing was wired for one call site out of many.** `run_slot` failures that map to a terminal phase also exist at `workflow/plan.rs` (planner, test-author) and `workflow/arena.rs` (reconciler, reconcile-review) — all now route `quota_hit` -> `Phase::Quota`/exit `4` the same way; the operator's own fleet pins planner/test-author to the same claude provider the incident hit, so this was not a theoretical gap. The implement loop's tester and reviewer dispatches (`implement.rs`) don't map to a terminal phase at all (a failure there reads as suite-inconclusive or `request_changes` and burns a fix round instead) — both now short-circuit to the same `fail()` when the discriminator fires, rather than retrying against a panel that cannot currently answer. (3) **the Claude-specific stated-reset parse was provider-blind.** `scrape_claude_rate_limits`/`scrape_claude_stated_reset` hardcoded the pause target as `"cli:claude"` regardless of which adapter's log was being scraped, so a codex/grok log that happened to say "resets " next to a limit phrase would pause claude instead of (or in addition to) the provider that actually failed; both functions now take the real `provider` and refuse to fire unless `normalize_key(provider) == "cli:claude"`, since the plain-text shape is genuinely Claude-CLI-specific output. (4) **`apply_parallel_outcome`** (`run_slots_parallel`'s true-concurrent path, used by `--workflow review`/`peer`, e.g. a live 2-reviewer panel) had no quota detection at all — both its failure arms now call the same `detect_and_pause_quota`, so a rate-limited slot there pauses the provider and records `quota_hit` on the slot; its callers have no `fail()`-style terminal-phase mapping today, so this adds detection/pause only, not phase routing — a real gap, left for the caller. **Resumability and cooldown parsing are unchanged from round 1**: `run_from_approved` accepts `Phase::Quota` as resumable, and `scrape_claude_stated_reset`/`parse_stated_reset` resolve Claude's plain-text "resets 12am (America/New_York)" line via `chrono-tz` to the next UTC occurrence of that local time, falling back to the generic default cooldown (with a visible stderr warning) when the reset can't be parsed. That fallback ceiling is worth naming: the parsed reset is the *next occurrence of the stated time-of-day*, capped at ~24h out, so a weekly window is re-probed daily rather than for days straight — a large improvement on the ~30-minute default, but not literally "waits out the week", since Claude's line carries no date. **Other adapters**: codex/opencode/muse/grok still have no adapter-specific rate-limit text handling (only claude, and separately agy via its own telemetry-based `quota_reset_secs`); a mid-dispatch limit on any of them is caught by `scrape_strong_quota_signal`'s generic rejection-phrase patterns and now routes correctly, just without a parsed cooldown — falls back to the generic default. **End-to-end coverage**: `tests/scenarios/quota_routing.rs` spawns the real `spar` binary against a fake `claude` CLI on `PATH` (no `--dry-run`) that prints the real captured incident text and exits 1, asserting `Phase::Quota`/exit `4` and that a follow-up `implement --run` is accepted; a second test does the same with an ordinary panic and asserts `Phase::Failed`/exit `1` and refusal. Earlier coverage only exercised each function in isolation (log -> bool, bool -> phase, hand-edited `state.json` -> resume) and never the chain through a real dispatch, which is the seam a routing bug hides in. **Left out of scope, deliberately**: `try_rotate_reviewer_provider`'s replacement pick doesn't consult the quota store, so a rotation could in principle land on another paused provider; and the reconcile/reconciler review-verdict paths (`request_changes`/unparseable verdict, not a dispatch failure) are untouched, since neither is a `run_slot` failure. **Round 3, driven by review, fixed four more things and corrected two overclaims.** (1) **`--workflow review`'s terminal-phase mapping ignored `quota_hit` entirely** (`workflow/review.rs`'s `any_failed && approve == 0 && changes == 0` always chose `Phase::Failed`) — the sharper bug underneath: `executor::salvage_expected_artifact` writes a synthetic `## Verdict\nrequest_changes` for *every* interrupted reviewer regardless of why it died, so `changes` is never `0` once any reviewer fails and the tally-based branch was already dead code before this round touched it, for *any* reason a reviewer fails, not just quota. Driven, not read: a live 2-reviewer `--workflow review` run against a fake `claude` printing the real captured incident text previously finished `Phase::Done`/exit `0` with a tally of two fabricated `request_changes` votes — a rate-limited panel silently reporting a normal (if negative) review. Fixed by checking `quota_hit` *ahead of* the vote tally: when every slot failed and every one of those failures was independently quota-detected, the run parks at `Phase::Quota`/exit `4`; a mixed panel (one genuine defect, one rate limit) still falls through to the pre-existing tally logic rather than being swept into either bucket by a majority-quota heuristic. The adjacent, pre-existing bug this exposes — an *ordinary* all-reviewer failure (a real panic, no quota) also reads `Phase::Done`/exit `0` today, because the same salvage-inflated tally masks it too — is **not** fixed here: it is a `workflow/review.rs` design problem (the tally can't distinguish a real review from a fabricated placeholder) orthogonal to quota routing, and fixing it would mean redesigning the aggregation, out of this task's scope. Named so it isn't mistaken for settled. `--workflow peer` still has no failure-to-phase mapping of any kind (`Phase::Done` unconditionally) — a pre-existing, larger gap, left untouched and no longer lumped in with `review`'s fix in the docs. (2) **The generic pause was writing under a key no reader looks up.** `detect_and_pause_quota`'s `scrape_log_hint` branch called `store.pause_quota(provider, hint)` with the raw provider string, while every reader (`is_usable`/`ensure_usable`/`filter_usable`) queries `normalize_key(name)`; for a pinned ref (`cli:codex@gpt-5.6-terra`, the fleet's normal shape per "pin the model on every role") the pause landed under a key `ensure_usable` never asks for, so the pause was inert for every non-claude adapter in practice. Fixed: `detect_and_pause_quota` now normalizes once and writes both branches under that key. Claude escaped this by accident (its structured branch already wrote the normalized `"cli:claude"`). (3) **The routing bool could fire on bare usage telemetry with no failure in the log.** `scrape_claude_rate_limits`'s `rate_limits.five_hour` JSON branch matches on `used_percentage >= 95` alone; since `detect_and_pause_quota` returned `true` unconditionally on any match from that function, an implementer that panicked while the window happened to read 96% used would have parked a real defect on the quota gate — the exact false positive the task names as worse than the bug being fixed. Routing now excludes that branch: it still drives the (harmless, auto-recovering) pause, but the returned bool comes only from `scrape_strong_quota_signal` or Claude's plain-text stated-reset line (an unambiguous rejection statement, not bare telemetry). (4) **`parse_stated_reset` had two robustness bugs**, both fail-safe (they degrade to the documented unparseable-fallback or a bounded time error, never silently wrong-route) but worth closing since they were one-line fixes: the `(timezone)` search scanned from "resets " to the end of the whole tail log rather than the matched line, so an unrelated parenthesis later in an 8000-byte log could be picked up as the zone; and the roll-to-next-day arithmetic added `Duration::days(1)` (a flat 24h) to a zoned `DateTime<Tz>` rather than advancing the calendar date, which drifts by an hour across a DST transition. **Two overclaims corrected, not code**: the weekly-reset cooldown is unchanged from round 2 (next occurrence of the stated time-of-day, capped ~24h, not the true weekly rollover — considered widening this to a ~7-day wait and rejected: Claude's line states no day-of-week, so a multi-day extension would itself be the "guessed duration" the task says not to fabricate, whereas the current daily re-probe self-corrects onto the real reset day within at most a week and is a straightforward reading of "parse the stated reset... and set the cooldown from it"). And the round-2 claim that a mid-dispatch limit on codex/opencode/muse/grok "is caught by `scrape_strong_quota_signal`'s generic rejection-phrase patterns and now routes correctly" is corrected to **unverified**: no adapter installs any rate-limit-specific handling (checked: `src/providers/{codex,opencode,muse,grok}.rs` have zero rate-limit/429/quota-related code), so a mid-dispatch limit on any of them does reach the same generic `bail!` path claude's does and is detectable only by `scrape_strong_quota_signal`'s phrase list — but that list has never been checked against real captured output from any of those four CLIs (the round-1 codex test string was invented, not sampled), so whether the phrases actually match is an open question, not a settled one. `cli:agy` is unaffected either way: it already has its own, separate telemetry-based cooldown (`quota_reset_secs`/`quota_remaining_fraction`, unrelated to log scraping) predating this fix. **Round 4, driven by review, fixed a critical regression round 3 introduced and closed three of the gaps round 3 named but left open.** (1) **CRITICAL — `Phase::Quota` resumability was made unconditional, so `implement --run` could bypass the plan-approval gate.** `run_from_approved`'s `resumable` check added `|| state.phase == Phase::Quota` with no companion check on `state.gates.plan_approved`, but `workflow/plan.rs` also parks a run at `Phase::Quota` before it is ever approved (the pre-dispatch gate, or a paused planner/test-author) — driven, not read: `spar plan -t "…"` against a fake claude that fails the pre-dispatch gate parked at `quota`/`plan_approved: false`, and a follow-up `spar implement --run <id> --dry-run --json` walked the whole implement loop through to `awaiting_ship_confirm` on a salvaged placeholder plan.md whose test-author never ran — before this bug, that command refused with "plan is not approved (phase=Quota)". Fixed by qualifying the clause: `state.phase == Phase::Quota && (state.gates.plan_approved || state.workflow == WorkflowKind::Loop)` — a `Loop` run (`--workflow loop`/`implement -t` directly) has no approval step at all, so its own quota park stays unconditionally resumable; every other workflow's quota park now requires the plan already approved. The correct resume path for an unapproved plan-phase quota park was already `spar plan --run <id>` (`workflow::plan::replan` already accepted `Phase::Quota`, unchanged by this fix) — this closes the accidental second door, it does not remove the real one. New regression test: `implement_refuses_a_quota_parked_plan_that_was_never_approved` (`tests/scenarios/plan_implement.rs`), and `implement_resumes_from_quota_phase` still passes since it starts from an *approved* plan. (2) **`--workflow review`'s mixed-panel guard was dead code, contrary to its own comment.** The comment above the terminal-phase mapping already claimed a mixed panel (one genuine defect, one rate limit) "still surfaces as `Failed` rather than hiding the real failure behind the quota gate", but the code branch it described (`!failed.is_empty() && approve == 0 && changes == 0`) could never fire: `salvage_expected_artifact` writes a synthetic `request_changes` for every interrupted reviewer, so `changes` is never `0` once any reviewer fails, quota or not — the same dead-code fact round 3 already found and left alone for the *ordinary*-failure case. Fixed by branching on `all_failed` directly (dropping the always-false `changes == 0` guard): every slot quota-hit still parks at `Phase::Quota`; every slot failed for any other or mixed reason now genuinely reaches `Phase::Failed`; anything short of all slots failing still reaches `Phase::Done` on its live vote tally. This was one guard, not a tally redesign, and it fixes both the mixed-quota case and the plain all-genuine-failure case round 3 named as out of scope — the fix for one was the fix for the other. New tests: `review_workflow_mixed_quota_and_genuine_failure_fails_the_run`, `review_workflow_ordinary_failure_on_every_reviewer_fails_the_run`. (3) **`--workflow peer` and `--workflow roles` had no failure-to-phase mapping of any kind**, named as a pre-existing gap in rounds 2/3 and left alone. Closed only the quota-specific slice, matching `review`'s narrower fix, not a general redesign: both now check, after their dispatch, whether every slot failed and every one of those failures was independently quota-detected, and if so park at `Phase::Quota`/exit `4` instead of finishing `Done`/exit `0`. Any other failure combination in either workflow still finishes `Done` — that larger gap is unchanged and still out of scope. `roles` dispatches sequentially through `run_slot` and was discarding its `Result` entirely (`let _ = executor::run_slot(...)`); the quota check reads `SlotStatus`/`quota_hit` back off `state.slots` after the loop rather than threading the discarded `Result`, since `run_slot` already records both on the slot before returning `Err`. New tests: `peer_workflow_rate_limited_on_every_slot_parks_on_quota_and_exits_4`, `roles_workflow_rate_limited_on_every_slot_parks_on_quota_and_exits_4`. (4) **Two round-3-named minor robustness gaps closed.** `run_slot`'s three backend-`Err` arms (api/tmux/headless spawn failure, distinct from a completed dispatch that exited non-zero) did not call `detect_and_pause_quota`, asymmetric with `apply_parallel_outcome`'s `Err` arm which does; now all three do, reading whatever the log captured before the backend errored out. `parse_stated_reset` anchored on the *first* "resets " occurrence in the whole tail log rather than scanning for one that actually parses as a complete `<time> (<tz>)` clause; an unrelated earlier "resets " (e.g. an unrelated status line) could make a real reset line further down go unparsed and silently degrade to the generic cooldown — fixed to scan line by line and return the first line that parses fully. New test: `parse_stated_reset_skips_an_earlier_unrelated_resets_line`. **Not re-litigated**: the weekly-reset cooldown (next occurrence of the stated time-of-day, capped ~24h) was flagged again this round as "fabricating a duration" by one of two independent reviewers; the other reviewer and this round both re-confirm round 3's reasoning stands — Claude's line states no day-of-week, so a multi-day extension would itself be the fabrication the task warns against, and left unchanged. `scrape_strong_quota_signal`'s two bare-phrase patterns (`"out of credits"`, `"quota exceeded"`, no paired rejection word) were flagged as a theoretical self-match risk since those literal strings now live in this module's own source and in these docs; left unchanged — bounded blast radius (the park is resumable, `state.error` still carries the real error) and both phrases already read as complete rejection statements on their own, so requiring a second paired word would weaken true-positive detection for no clear safety gain. The codex/opencode/muse/grok adapter-text gap named in round 3 is unchanged (still no adapter-specific handling, still only `scrape_strong_quota_signal`'s generic list, still unverified against real captured CLI output) — added one end-to-end test (`non_claude_provider_generic_rate_limit_phrase_still_parks_on_quota`, a fake `cli:grok` failing with a generic "429 too many requests" line) proving the generic path is wired end-to-end for a non-claude provider, which is not the same claim as verifying any adapter's *actual* wording | DECIDED |

## Model select (vals-backed)

Opt-in path: resolve fleet slots from benchmark data + user profiles + per-run urgency, instead of only manual `--providers`. Explicit `--providers` remains valid and default-required until select is opted in.

| ID | Decision | Status |
|----|----------|--------|
| MS0 | Data source: **vals.ai** coding benches; abstract behind `BenchmarkSource` so an official API can replace HTML scrape later | DECIDED |
| MS1 | Per-model fields used: **accuracy**, **latency**, **cost_per_test** (from vals payload) | DECIDED |
| MS2 | Phase A primary bench: **SWE-bench Verified** only; later optional blend (Terminal-Bench, LiveCodeBench, Vibe Code, Vals Index) | DECIDED |
| MS3 | Selection is **opt-in**: keep `--providers` required unless `--select <profile>` and/or `[model_select]` enables auto | DECIDED |
| MS4 | Named **profiles** in config (`best` / `value` / `fast` or custom) with weights for quality / cost / speed + optional `min_accuracy` | DECIDED |
| MS5 | **Urgency** is a per-run multiplier on the chosen profile (high → speed↑ cost↓; low → cost↑), not a separate parallel system | DECIDED |
| MS6 | Score: normalize metrics in candidate set; `score = w_q·acc − w_c·cost − w_s·latency`; apply floors/allowlists before rank | DECIDED |
| MS7 | **CLI economics**: treat `cli:*` cost as **0** for scoring (flat sub); do not use vals $ against subscription CLIs — superseded by MS15 for `@model` refs | DECIDED |
| MS8 | Resolve to **assignable slots** (`ProviderRef` + optional model), not abstract model names only; filter by doctor-available backends | DECIDED |
| MS9 | Multi-slot fleets (plan/arena/review ≥2): prefer **provider-family diversity**, then next-best score | DECIDED |
| MS10 | Always write **`artifacts/model-select.json`** (candidates, weights, urgency, winners, why) into the run | DECIDED |
| MS11 | Cache: `~/.spar/cache/vals/…` + TTL; `spar model refresh`; stale cache usable within grace window; fail closed with clear error if no cache and fetch fails | DECIDED |
| MS12 | Role defaults in config (`planner`/`implementer`/`reviewer`/`tester`/`critic` → profile names); suite `tester` defaults toward **fast/value** | DECIDED |
| MS13 | CLI surface: `spar model list|pick|refresh`; doctor reports cache age | DECIDED |
| MS14 | Ship phases: **A** cache+parser+list/pick · **B** `--select` into plan/implement · **C** roles/urgency/diversity · **D** per-adapter CLI model flags + richer vals→spar map | DECIDED |
| MS15 | **Revises MS7 for `@model` refs.** `cli:*` is zero-cost only when the adapter bills against its own subscription. `cli:codex@<openrouter-slug>` is a `cli:` ref that routes to OpenRouter and bills **per token** (observed: 29.5k input on a trivial task; 489k on a real review run), so cost scoring must key on the **resolved model**, not the backend prefix. Recording the decision here; joining `turn.completed.usage` (already parsed into `stats.json`) to the OpenRouter price table is follow-up work | DECIDED |
| MS16 | **`spar model list --provider openrouter` filters to `supported_parameters` containing `tools` by default** (268/339 models); the remaining 71 cannot function as agents and are hidden unless `--all` is passed. Guardrail, not a preference | DECIDED |

## Workspace + bus delivery

Workspace initiative (2026-07-13). See `roadmap/workspace-initiative-plan.md` for the staged build.

| ID | Decision | Status |
|----|----------|--------|
| W1 | spar owns its **own tmux server socket** (`tmux -L spar`); the user's personal tmux sessions are never touched | DECIDED |
| W2 | Live pane output via tmux **control mode** (`tmux -C` `%output` stream), not `capture-pane` polling | DECIDED |
| W3 | Embedded terminal rendering via `vt100::Parser` + `tui-term` as a new TUI focus target | DECIDED |
| W4 | "Own coding UI over direct API" **deferred** — use opencode as the harness for API-only providers for now | DECIDED (deferred) |
| W5 | Workspace bus keyed by `agent_id`; `run_id` demoted to an optional message tag; `SPAR_AGENT_ID` gives bare agents identity. Lands last | DECIDED |
| W7 | Terminal panel is a **real embedded workspace shell** (persistent `$SHELL` on the spar socket, rooted at the project — run dev servers etc., survives TUI restarts) by default; agent panes reachable by cycling (Ctrl+PgUp/PgDn). Extends W3 | DECIDED |
| W8 | Embedded Terminal is a **real tmux client via PTY passthrough** (`portable-pty` runs `tmux -L spar attach`; raw keys/mouse/paste forwarded, output rendered): full tmux — prefix, copy-mode/scroll, paste, splits, search. `F12` returns focus to spar. Supersedes the W7 custom pane-switcher. Extends W3/W7 | DECIDED |
| W9 | **Agent takeover** in the Terminal: selecting a slot in the Agents pane attaches the passthrough terminal to that run's `spar-<run_id>:<slot>` pane so you can view + drive the agent directly. Requires the run to have used `--backend tmux` (headless has no pane); `Ctrl+b d` / session-end hands focus back to spar. Extends W8 | DECIDED |
| B1 | `@human` routing = always-on TUI sink (baseline, zero config) + opt-in generic `[notify]` command/webhook; **no hardcoded personal integrations** in source | DECIDED |
| Z1 | **On-disk markers are ground truth for slot status at the read boundary.** `status` / `list_runs` / TUI reconcile a `running` slot that has a `<slot>.done` / `.failed` marker to that verdict (`state::reconcile_slot_status`). In memory only — observe-only commands never rewrite `state.json` | DECIDED |
| Z2 | **"Abandoned" is a computed read-time flag, not an exit code.** A run in a non-terminal, non-gate, non-`Stopped` phase with no live `RunLock` owner is abandoned (`state::is_abandoned`); surfaced in `status`, `status --json` (`"abandoned"`), and the TUI. The `0/1/2/3/4` exit-code contract is untouched | DECIDED |
| Z3 | **`spar cleanup` reaps by cwd, never by command line.** Processes are matched via `/proc/<pid>/cwd` inside the run's own worktrees (self + ancestors excluded), SIGTERM → grace → SIGKILL, then the worktree is removed. Matching on command line self-matches the caller's shell | DECIDED |
| W6 | **Image paste over SSH = local-companion bridge (option A).** A local `spar clip` reads the OS clipboard image (`arboard` / `wl-paste` / `pbpaste`), ships raw bytes to the remote spar over the existing SSH connection (ControlMaster-exec or a forwarded socket); the remote stages a `0600` temp file and injects its **path** into the agent pane via Track A `send-keys` (agents accept image file paths). Additive; fits the ssh-then-run + `tmux -L spar` model. Thin-client alternative deferred — see X7 | DECIDED — backlog feature 002 |

## TUI

TUI restructure (2026-07-14). See `roadmap/tui-redesign-plan.md` for the staged build.

| ID | Decision | Status |
|----|----------|--------|
| U1 | TUI is a **rail + one main area** (content = f(selection × tab)), not N co-equal Tab-cycled panels. Rail is a `projects ▸ runs ▸ agents` drill-down (`Enter` pushes, `Esc` pops, breadcrumb in a single status line); Log/Activity/Diff/Shell are **tabs over Main**. Focus targets cut 6 → 3 (Rail/Main/Composer) with direct keys `1/2/3`. Chrome cut from 10 rows to 2. Researched against k9s / lazygit / lazydocker / herdr / claude-squad, which all use this shape; an N-way focus ring destroys spatial memory | DECIDED |
| U2 | The embedded terminal is a **mode, not a panel** — it lives in Main's Shell tab (Stage A) and becomes a full-screen Driving mode (Stage B). Escape is a prefix (`C-a`) + `F12`, never `Esc`/`Tab` (the agent needs those — Shift+Tab is Claude Code's permission toggle) | DECIDED |
| U3 | Stage B: the `:` **command palette replaces the Composer focus target** — focus is 2-wide (Rail/Main, keys `1`/`2`); the palette is a transient overlay, not in the ring. Its verbs are the run-lifecycle actions the orchestrator already brokers (`approve`/`reject`/`ship`/`confirm`/`reconcile`/`takeover`/`spawn`/`chat`) plus `implement`/`plan`, which spawn the detached CLI **reusing the selected run's recorded `providers`** — a fresh fleet needs a provider picker a text palette can't offer, so those error to the CLI. All complete run ids from the roster | DECIDED (fresh-fleet punt superseded by U8; retired in code by 004 Phase D — `plan` with no run selected opens the new-run surface instead of erroring) |
| U4 | Stage B: **`q` is the quit path**; double-`Ctrl+C` retired so `Ctrl+C` is unambiguously the agent's SIGINT in the Shell tab. `/` filters the rail (dim-in-place + match-only navigation; hiding rows would desync the selection index that feeds the snapshot). Diff renders the selected slot's real `git diff HEAD`, capped, falling back to artifacts when a slot has no worktree | DECIDED |
| U5 | Stage C: **attention model.** Each run gets a cheap `Attention` level from its summary (`Gate > Broken > Working > Idle`); the rail is **attention-sorted** (loudest first, then recency) with selection **glued to the run id** across re-sorts. A run that wants the operator flies a `⚑` and rolls up to its project row and a status-line `⚑N need you` count. **`a` jumps to the next run that needs you** (approve loses its one-key shortcut — tap the button or `:approve`); tapping the roll-up does the same. A run crossing into Gate/Broken **toasts** (first snapshot primes silently). Width bands: `<80` Main-only · `80–119` rail+Main · `>=120` the extra width goes to Main, never a 4th box | DECIDED |
| U6 | Noun set is **Project / Run / Agent / Shell**. "Session" is retired as a user-facing term and survives only as a tmux implementation detail (`spar-<run_id>`). `docs/PRODUCT.md:43` "Session / run home" becomes **Home** | DECIDED |
| U7 | **Home is the landing view**: what needs me (ranked by wait time) / running / finished since last look / start something new. Drill-down survives as navigation, not the front door. Promotes U5's attention level from a rail decoration to the organizing principle. Extends U1 | DECIDED |
| U8 | **New-run flow** from Home (`n`): a brief field written to `.spar/briefs/<slug>.md` plus a fleet picker over the roster. Supersedes U3's punt ("a fresh fleet needs a provider picker a text palette can't offer, so those error to the CLI"): build the picker | DECIDED — brief field superseded by U16 (it becomes a conversation) and its manual replacement decided as U21 (a one-line task); the fleet picker was built in 004 Phase D and survives as the manual path that costs no tokens |
| U9 | **Gates render their evidence.** Plan tab shows `plan.md`, the critique and `test-contract.md` as documents; Review tab shows per-`AC-n` pass / fail / unverified, reviewer verdicts and the diff. A gate that cannot show the acceptance predicate it enforces (O19, relaxable for `unverified` via `require_all_criteria`) is a rubber stamp. Extends U4 | DECIDED |
| U10 | **Motion is time-based** (`Instant`, easing curves, `Tween<T>`, enter/exit transitions), never tick-modulo. The frame clock ramps to ~16ms while `app.animated` is set and idles low otherwise; `FRAME` at a flat 100ms reads as a slideshow | DECIDED |
| U11 | **Reserve space from the layout, never from the content.** Content clips into fixed slots, skeletons hold space before data lands, reorder is an animated transition. Kills all four current shift sources: width-summed gate buttons, attention re-sort, growing lists, the width-breakpoint tab strip | DECIDED |
| U12 | **Design tokens, not scattered consts**, one accent and one alert colour held to strictly, a consistent border and weight language, braille/half-block density where it earns its place. Stability is asserted, not vibed: a `TestBackend` snapshot harness covers 20x5, empty project list, 400 runs, and "this region does not move when that value changes" | DECIDED |
| U13 | **`draw` never touches disk.** Every read is the refresher thread's job and arrives on `Snapshot`. Today `rail_project_items` calls `registry::list_visible_project_runs` per project per redraw (`src/tui.rs:2830`), a real render-path scan, while the refresher already does the equivalent work correctly off-thread inside `build_snapshot` (`src/tui.rs:873`) | DECIDED |
| U14 | **The chrome is bands and rules, not boxes, and the background belongs to the terminal.** Pane borders are gone: a header, a run stepper, a labels row and one rule sit above a body split by a single seam, with the active tab marked by an accent underline on that rule (a tab indicator that costs no row). spar paints no page background, so it composites onto the host theme and its transparency; backgrounds appear only on chips, the gate/alert washes and overlays. Tokens live in `src/theme.rs` (U12's token set, one accent + one alert + three text weights). The stepper's step table is keyed on `WorkflowKind` (arena and roles runs have different fleets, and one fixed table shows them steps that never existed), its states come from the slots that ran, and it distinguishes *skipped* (disabled channel, unused role) from *pending*; the band's meters read `state.usage`, the run's ledger, so they agree with `status --json` rather than under-reporting every re-dispatch. The rail's width comes from the terminal width alone and an agent's identity is its **role**, never the provider-suffixed slot id. **Amends U5 and U1.** U5 gave the `>=120` band's extra width entirely to Main; the rail now takes 6 of those columns (26 → 32) because role + model is what makes an agent identifiable and 26 columns cannot hold both — the rest still goes to Main and there is still no fourth box. U1 budgeted 2 chrome rows; there are now 5 (header, stepper, labels, rule, footer), but the two pane borders they replaced cost 2 rows and 4 columns, so the net loss is one content row and the net gain is 3 columns. The gate zone is reserved on 80 columns and up; below that (phone width, no rail) buttons still right-align from summed widths. Landed **before** U7's Home and U9's gate evidence, reversing the doc's 004 → 005 → 006 order for the chrome half of 006: the argument for ordering ("animating a confusing layout gives you a confusing layout at 60fps") holds for motion, not for chrome, and building Home into the old shell would have meant building it twice. U10's motion work keeps its place after 005 | DECIDED |
| U15 | **The run list shows units of work, not invocations.** A run with `parent_run` set is a *leg*: the rail folds it into its parent's row, which carries the parent's identity (id and brief), the most recently updated leg's phase, and the loudest attention level of the group — folding must never hide a gate, which is the exact failure O36 exists to prevent. Rounds are shown, not slots-per-round: the agents level stays fleet-sized (re-dispatch reuses slot ids), and the context band names the current round when a run has more than one. Extends U7's Home ranking, which ranks these same units | DECIDED |
| U16 | **The TUI hosts a resident orchestrator conversation.** Settles X10 yes and supersedes U8's brief field: `n` on Home opens a conversation, not a text box. The orchestrator is an **Agent** in U6's noun set, not a fifth noun — W5 already makes a bare spawned agent bus-addressable by id — and its surface is a fifth Main tab whose content is `f(rail selection)` like every other tab: a selected run scopes the conversation to that run, Home scopes it to a new one. It is precisely the disposable session P7 already describes, hosted inside spar instead of outside it: its prompt is the core skill already embedded in the binary (`src/skills.rs`), it carries `SPAR_PROJECT_ROOT` / `SPAR_RUN_ID` / `SPAR_AGENT_ID` like every slot (`providers/presence.rs`), it gets current by reading `.spar/runs/<id>/`, and it drives the same CLI under the same exit-code contract an outer agent drives. Killing it and spawning another loses nothing, because the system of record is still on disk. Three jobs, all the same capability: **intake** (interview → `.spar/briefs/<slug>.md` → fleet proposal → launch), **gate consultation** (grounded in U9's rendered evidence, which it reads rather than replaces — a conversation about a gate is only as good as the criteria grid behind it), and **status**. The `:chat` palette verb (a raw bus message) is renamed off the collision | DECIDED |
| U17 | **The orchestrator proposes, the operator disposes — and a turn is a backend concern.** The daemon prohibitions in `docs/architecture-operator-model.md` (never approve a plan, never confirm ship, never merge, never sweep `cleanup`, never choose a fleet) bind the conversation too: "a supervisor is not an operator" is a claim about authority, not about which process happens to hold it. The orchestrator drafts a brief, proposes a fleet and argues a gate either way; the commit is the human's, through the gate buttons and palette verbs that already exist. On the second half: the orchestrator layer owns "there is a conversation producing a brief, a fleet and a launch" and the **backend** owns how a turn yields, so nothing forks inside a workflow. `native-cli` yields by dispatching headless with the transcript in the prompt and addressing its question to `@human` (X4's turn-boundary delivery, zero new execution machinery, rides the subscription, cold context every turn). `api-sdk` yields by holding the message list in `src/api/runtime.rs` and streaming (cheap turns, real token streaming, needs an `api:` key and an Anthropic messages lane that does not exist yet). native-cli ships first | DECIDED |
| U18 | **Home is a rail level** (`BrowseLevel::Home` is the root), not a new pane. `Projects` survives as navigation reachable by `p` or a Home row; a Home run row's `Enter` opens that run's Agents directly (switching `active_root` to the row's project and carrying the run by id, `App::home_target_run`, across the one-tick snapshot handoff), then `Esc` exposes that project's Runs and a second `Esc` returns to Home. Extends U7/U1 | DECIDED |
| U19 | **Home's "finished since last look" watermark is a global file** under `registry::spar_home()` (`home_watermark.json`), because Home is cross-project and `.spar/` is per project. Read once at `App::new` and held for the whole session so band 3 does not empty under the operator while they are looking at it; written on clean exit. A missing or corrupt file reads as a day ago — nonfatal, and it only ever makes band 3 show more, never fewer | DECIDED |
| U20 | **Home scope**: `spar` inside a repo lands on Home scoped to that project (`HomeScope::Project`); outside a repo it is every registered project (`HomeScope::All`). `P` toggles. Bands, order and the four headers are identical either way — scope filters rows, it never changes the view. Settles the scoping U7 deferred | DECIDED |
| U21 | **The 004 new-run surface takes a one-line task** (`plan -t <task>`), not a brief file. The brief file is U16's half and lands with 008's conversation; the task line plus the fleet picker (`NewRun`/`RosterEntry`/`build_roster`/`new_run_launch`) is the token-free manual path U16 requires to survive. Amends U8 | DECIDED |
| U22 | **The provider roster is snapshotted when the picker opens**, not per frame — `providers::detect_all()` resolves binaries on `PATH`, which is disk (U13). It is also not built inline in the key handler: `detect_all()` spawns every provider with `--version`, so a stuck binary would freeze `n`, `:plan` and `spar --task`'s startup. `begin_new_run` opens the modal immediately in a "checking roster" (`loading: true`) state and hands the probe to a background thread tagged with a `gen` counter; `Msg::RosterReady(gen, roster)` only applies if the open modal is still on that generation, so a cancelled or reopened modal can't be clobbered by a stale result. `NewRun.roster` is read-only once populated | DECIDED |
| U23 | **Cross-project Home/Projects invalidation is off-thread and bounded.** The active project's marks keep their 200ms `REFRESH` cadence; Home/Projects gathers cross-project marks (`cross_project_marks`) at a separate, coarser `CROSS_PROJECT_REFRESH` (2s) cadence gated by `cross_project_due`, with an immediate forced sweep on entering Home/Projects or toggling scope. `Runs`/`Agents` never trigger it. This prevents U13's thousands-run registry scan from becoming a permanent per-tick stat sweep | DECIDED |
| U24 | **One predicate decides "does this run need the operator": `wants_operator`.** Round-9 review found `build_home_rows` banding `PlanApproved` into NeedsMe while `runs_needing_attention`/`fold_units`'s `wants`/the rail flag still went through `run_attention(r).needs_you()` alone, so Home could paint a NEEDS YOU row under a header reading `0 need you`, and the same row disagreed with the Projects-level `⚑` and the Runs-level rail flag. `wants_operator(r) = run_attention(r).needs_you() || r.phase == Phase::PlanApproved` is now the one predicate all four go through (`fold_units`, `runs_needing_attention`, `build_home_rows`, `attention_flag` shared by `rail_home_items`/`rail_run_items`, and the in-project `a` cycle). `run_attention` itself, `phase_color` and `sort_runs_by_attention` are untouched — round-8's carry-forward already rejected folding `PlanApproved` into `Attention::Gate` globally, since `PlanApproved` stays colored `OK` like `Done` everywhere outside this one "needs you" question | DECIDED |
| U25 | **`:plan <task>` with no run to reuse a fleet from targets through `new_run_target`, the same rule `n` uses** — not `swarm.project_root` (`active_root`), which lags a snapshot behind while the operator is browsing cross-project Home and can point at whichever project was entered first rather than the row under the cursor (round-9 review, major). `open_new_run` and the palette's `plan` fallback both call `new_run_target(app, projects, home_rows, local_root)` so they cannot drift apart again | DECIDED |
| U26 | **A Home cursor keys on identity that cannot move on its own, not on `id`.** Round-10/11 review (AC-28) found two: (1) `fold_units`'s `id` is whichever leg is currently loudest, which can swap between snapshots as attention shifts within a unit, so `home_row_key`'s `run:{id}` key moved out from under the cursor even though the unit was unchanged — fixed by a new `RunSummary.unit_id` (the fold root, `#[serde(skip)]`, set by `fold_units`), which `home_row_key` prefers over `id`. (2) `HomeRow::Project(usize)` keyed on its index into the registry's project list, which shifts when a project registers or drops — fixed by carrying the project's root alongside the index, `HomeRow::Project(usize, PathBuf)`, and keying on the root. Also: the first frame painted on startup built its snapshot synchronously on the UI thread (`build_snapshot` before the render loop), which is now cross-project every time because Home is the landing view (U18) — at the "thousands of run dirs" scale U13 exists for, that blocks the first paint on the same scan U13 keeps out of `draw`. `Snapshot::loading` (empty, no disk I/O — `SparPaths::new` only joins paths) now paints frame one; the refresher thread's forced first iteration replaces it within one `REFRESH` tick. Separately, a static Home (no selected run, so `animating()` never fires) never repainted on a plain clock tick, so its `relative_wait`/`relative_age` labels froze at the last real rebuild instead of counting up — `HOME_CLOCK_TICK` (5s) now forces a repaint at that cadence while at Home and nothing else made a frame dirty | DECIDED |
| U27 | **U26's `unit_id` fix only reached the cursor key; the pin and the on-screen text still used `id`.** Round-11 review (major) found `home_target_run` (set by a Home `Enter`) and both Home renderers still used `run.id` — the loudest leg — so a unit whose loudest leg changed between the Home snapshot and the destination project's own snapshot would (a) visibly change its displayed 8-hex id out from under the operator and (b) never be found by `resolve_home_target`'s `id`-only search, eventually giving up and bouncing back to Home. Fixed by routing every "which id is this Home row" question through one helper, `home_display_id` (`unit_id` when folded, else `id`): `rail_enter` pins `home_target_run` to it, `resolve_home_target` matches on `unit_id` first (falling back to `id`), and `rail_home_items`/`home_overview`/the `a`-cycle flash all render it instead of `run.id`. `run.id` itself is untouched — it still names the actionable leg for phase, gate actions and the `RunState` an Agents drill-down loads, which is deliberately allowed to lag by one tick while a pin is being resolved, matching the existing R2 tolerance. A second round-11 finding (major): `new_run_target` (U25) only ever consulted `home_scope`/`home_rows`, both blank once the operator has drilled past Home into a project's Runs/Agents view, so `n`/`:plan` there silently retargeted to whatever `home_scope` last was — with a scoped Home, retargeting to a *different* project with no way to correct it via `←`/`→`. `new_run_target` gained a `browsed_project: Option<&Path>` that wins over `home_scope`/`home_rows` whenever `BrowseLevel::in_project()` is true, sourced from `active_root` (the `n` key) or `swarm.project_root` (the palette's `plan` fallback) — both already correct, non-stale reads of the project actually being browsed. Also: `home_row_key`'s `run:{unit_id}` had no project qualifier despite Home being cross-project and folding being per-project (U26/AC-34), so two projects whose 8-hex ids collide could glue the cursor to the wrong project's row — the key is now `run:{project_root}:{unit_id}`. Also: the clamp block in `run_loop` only called `resolve_home_target` when `snap.runs` was non-empty, so a Home `Enter` into a project whose Runs listing had not landed yet (R2's one-tick lag, but with an *empty* rather than merely different listing) never ticked `home_target_ticks` and pinned forever — reordered so `resolve_home_target` runs whenever `home_target_run.is_some()`, before checking emptiness. Also: `Snapshot::loading` now seeds `home.rows` via `build_home_rows(&[], &[], ...)` (pure, no disk) instead of leaving it empty, so the four band headers and the `n` CTA are on screen from frame one rather than blank for one `REFRESH` tick | DECIDED |
| U28 | **An alert the operator cannot act on is a bug, not a warning.** A folded unit (U15) reports that it wants the operator through `unit_wants_operator`, but the row it renders acts on a single leg, so "needs you" must never name a unit whose representative leg offers no action. Two rules make that structural rather than incidental. **First, `PlanApproved` is conditional.** It is a handoff only while nothing is running: a unit whose plan is approved *and* which has an active leg has already been dispatched, so the approval is stale bookkeeping and nobody is waiting. `Gate` and `Broken` are unconditional — a working sibling does not clear them. **Second, `fold_units` orders by attention first and `wants_operator` only as a tiebreak within one attention level.** Attention must dominate or a `PlanApproved` unit (which scores `Idle`) sinks below every working run in the attention-sorted rail and gate buttons retarget off the leg that holds the gate; the tiebreak then only reorders legs that already sort equally, which is exactly the `PlanApproved`-versus-`Done` case that left the row unactionable. Arrived at through two rejected operator fixes (runs `e72f434e`, `3be317b2`): the first retargeted the row and sank the unit, the second flagged a unit with nothing to press. Amends U15, which says folding must never hide a gate, by adding that it must not invent one either | DECIDED |

## Open

| ID | Topic | Status |
|----|-------|--------|
| X1 | First API provider to spike (xAI / Anthropic / OpenAI) | DECIDED — OpenAI-compatible (`api:openai`, `api:xai`) via ureq |
| X2 | TUI keymap / layout (mimic which product most?) | DECIDED — rail + one main area, k9s/lazygit shape; see U1/U2 |
| X3 | Project template overrides day one vs later | OPEN |
| X4 | Bus steer reliability for native-cli headless | DECIDED — adapter-dispatched turn-boundary delivery (Stop-hook inject / native queue / SDK prompt / inbox-on-next-turn per adapter) |
| X5 | vals scrape parser brittleness / grace TTL days / ship in-repo snapshot | OPEN — decide at MS phase A impl |
| X6 | Exact vals model id → `cli:`/`api:` + model string mapping table | OPEN — phase A/B |
| X7 | **Thin-client split** (`spar --remote`: local TUI ↔ remote orchestrator over spar's own protocol stream) as an alternative to ssh-then-run — would make image paste fall out as one message type (herdr's model) and give attach/persistence without tmux. Reopens the tmux-vs-own-protocol call (W1/W2) | OPEN — backlog; revisit after the workspace initiative lands |
| X8 | ProviderRef `@model` split + precedence | DECIDED — split on **first** `@` before the colon check (model may carry `:`/`/`, name may not); `display()` round-trips with `@model`, `storage_key()` strips it (quota bucket + slot.provider stay model-free so `@opus`/`@haiku` share one bucket); slot.model = `pref.model.or(explicit)` so an explicit `@model` beats a `--select` choice |
| X9 | Resolving the driving agent's tmux pane by walking `/proc` ancestry | REJECTED (verified working, then dropped: it exists only to compensate for a session being load-bearing, which P7 removes. If an agent must mind a run, spar spawns it on its own socket (W1) where pane resolution is definitional) |
| X10 | Whether the TUI ever hosts a conversation | DECIDED — yes, see U16 / U17. The target flow still does not *require* one (the fleet picker and the CLI remain token-free paths), but the operator asked for the disposable briefing session to live inside spar rather than outside it, which is the one job P7 already assigns to a session |
